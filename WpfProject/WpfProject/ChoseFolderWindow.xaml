<Window x:Class="WpfProject.ChoseFolderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:v="clr-namespace:WpfProject.View"
        Title="ChoseFolderWindow" Height="170" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <v:NewFolderView/>
        
        <WrapPanel HorizontalAlignment="Right" Grid.Row="1" Margin="15">
            <Button IsCancel="True" x:Name="CancelButton"  Content="Cancel" MinWidth="50" Margin="10"/>
            <Button IsDefault="True" x:Name="AcceptButton" Click="AcceptButton_Click"  Content="Create folder" MinWidth="50" Margin="10"/>
        </WrapPanel>
    </Grid>
</Window>

